import styled from "styled-components";

export const ViewStyles = styled.div`
  grid-column: 2 / span 10;
  display: grid;
  grid-template-rows: repeat(3, max-content);
  justify-items: center;
  margin: 6.5rem 0 11rem 0;
`;